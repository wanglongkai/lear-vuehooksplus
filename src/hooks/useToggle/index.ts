
/**
 *  * @description A custom hook that toggles between two values.
 * @param {boolean} initialValue - The initial value of the toggle.
 * @param {*} leftValue 
 * @param {*} rightValue 
 */
export function useToggle(leftValue: string, rightValue: string){

}